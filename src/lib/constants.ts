/**
 * @deprecated Import from @fossapp/core/config instead
 * This file is a re-export stub for backward compatibility.
 *
 * Migration:
 *   Before: import { VALIDATION, PAGINATION } from '@/lib/constants'
 *   After:  import { VALIDATION, PAGINATION } from '@fossapp/core/config'
 */

export {
  VALIDATION,
  PAGINATION,
  DASHBOARD,
  CACHE,
  UI,
  API,
  type ValidationConfig,
  type PaginationConfig,
  type DashboardConfig,
  type CacheConfig,
  type UIConfig,
  type APIConfig,
} from '@fossapp/core/config'
