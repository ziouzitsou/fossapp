/**
 * @deprecated Import from @fossapp/core/validation instead
 * This file is a re-export stub for backward compatibility.
 *
 * Migration:
 *   Before: import { validateSearchQuery } from './validation'
 *   After:  import { validateSearchQuery } from '@fossapp/core/validation'
 */

export {
  validateSearchQuery,
  validateProductId,
  validateCustomerId,
  validateProjectId,
  validateTaxonomyCode,
  validateSupplierId,
  validateUUID,
} from '@fossapp/core/validation'
