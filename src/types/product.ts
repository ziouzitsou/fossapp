/**
 * @deprecated Import from @fossapp/products/types instead
 * This file is a re-export stub for backward compatibility.
 *
 * Migration:
 *   Before: import { ProductInfo } from '@/types/product'
 *   After:  import { ProductInfo } from '@fossapp/products/types'
 */

export type {
  TemplateType,
  ProductSearchResult,
  ProductInfo,
  Price,
  Multimedia,
  Feature,
  FeatureGroupConfig,
} from '@fossapp/products/types'

export {
  MIME_CODES,
  ETIM_FEATURE_GROUPS,
  PRODUCT_DISTRIBUTION,
} from '@fossapp/products/types'
